{% extends 'base.html.twig' %}

{% block title %} Article Create {% endblock %}

{% block body %}
    <div class="container">
        {{ form_start(form) }}
            <div class="id">
                {{ form_label(form.id) }}
            </div>
            {{ form_widget(form.id) }}

        {{ form_end(form) }}
    </div>
{% endblock %}